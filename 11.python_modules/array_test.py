import array

# array in python take less memory and perform faster because list can freely add and remove which can cost more memory
# first param is type code of the python array
arr = array.array('i', [1,2,3,4,5])
print(arr)